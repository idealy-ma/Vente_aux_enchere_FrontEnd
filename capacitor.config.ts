import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.enchere',
  appName: 'Vente Enchere',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
